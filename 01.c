#include <stdio.h>
#include <locale.h>

int main()
{
  int M[10];

  for (int i = 0; i < M; i++)
  {
    printf("Insira o %dº valor: \n", i + 1);
    scanf("%d", &);
  }

  for (int j = 0; j < M; j++)
  {
    printf("%d ", M[j])
  }

  return 0;
}